ohctl regenerate — write stored transcripts again

Usage:
    ohctl regenerate [<session>...]

Sessions are named on the command line, or every stored session is done when the command names no session.
