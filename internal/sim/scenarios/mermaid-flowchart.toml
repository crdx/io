model = "gpt-5.6-sol"

strict = true
loop = false

delay = "600ms"
pace = "40ms"

[[turn]]
think = ["Mapping the request path."]
say = """
# Request processing

```mermaid
graph TD
request[Receive] --> validate[Validate]
validate --> accept[Accept]
validate --> reject[Reject]
subgraph audit [Audit trail]
accept --> recorded[Record success]
reject --> failed[Record failure]
end
```
"""
