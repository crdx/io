=== a panel grown taller than the terminal ===

 expose the five preview ports


Exposing each port in turn.
expose 8001 ✓
expose 8002 ✓
expose 8003 ✓
expose 8004 ✓
expose 8005 ✓


 🤖 Exposed sandbox port 8001 to the host at http://127.27.192.223:8001.
 🤖 Exposed sandbox port 8002 to the host at http://127.27.192.223:8002.
 🤖 Exposed sandbox port 8003 to the host at http://127.27.192.223:8003.
 🤖 Exposed sandbox port 8004 to the host at http://127.27.192.223:8004.
 🤖 Exposed sandbox port 8005 to the host at http://127.27.192.223:8005.


All five previews are up.
=== streamed taller than the terminal ===
Agent 000000000000000000000000 ready in 0s with 0 skills ⧸ 0 snippets ⧸ ~800t context.


 have a look at that


Taking the four of them together rather than one after another.

Let me get the shape of this first.

ls ✓ 7L
find **/*.go ✓ 2L
find **/*.toml ✓ 1L
grep package *.go ✓ 2L
Three searches, all at once.

Now to find where the drawing happens.

grep spinner *.go ✓ 1L
grep footer *.go ✓ 1L
grep cursor *.go ✓ 1L

One more pair, and one of them will not be there.

ls ✓ 7L
read nowhere/at/all.go ✗ 1L nowhere/at/all.go: no such file or directory

That is the lot: four together, then three together, then a pair where only one of them could be
answered.
