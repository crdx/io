model = "gpt-5.6-sol"

strict = true
loop = false

delay = "600ms"
pace = "40ms"

[[turn]]
think = ["Reading the renderer before saying anything about it."]
say = "Let me read the renderer."

[[turn.call]]
name = "read"
arguments = '{"path":"cmd/oh/edit/render.go"}'

[[turn]]
say = """
The prompt is built in cmd/oh/edit/render.go:4:9, and screen.go holds the spinner beside it. \
There is nothing about it in README.md, and nothing at all in cmd/oh/edit/missing.go, which is \
where I looked first. settings.toml names the workspace and target.txt is the file everything \
else works on.
"""
