=== released and kept ===
[startup=0s session=000000000000000000000000 skills=0p/0g tools=~600t]


 have a look at that


Looking for where the prompt is drawn.

I'll have a look at how the editor draws its prompt.

grep prompt *.go ✓ 1L ~100t
The prompt is drawn by the renderer, so that is the file to read.

Found it. Reading the renderer now.

read cmd/oh/line/render.go ✓ 5L ~100t

The prompt is drawn by layout, which wraps the buffer against the terminal width and reports where
the cursor landed. Nothing there needs changing to make it sticky: the work belongs in the output
layer instead.
=== released and unused ===
[startup=0s session=000000000000000000000000 skills=0p/0g tools=~600t]
=== resized ===
[startup=0s session=000000000000000000000000 skills=0p/0g tools=~600t]


 have a look at that


Looking for where the prompt is drawn.

I'll have a look at how the editor draws its prompt.

grep prompt *.go ✓ 1L ~100t
The prompt is drawn by the renderer, so that is the file to read.

Found it. Reading the renderer now.

read cmd/oh/line/render.go ✓ 5L ~100t

The prompt is drawn by layout, which wraps the buffer against the terminal width and reports where
the cursor landed. Nothing there needs changing to make it sticky: the work belongs in the output
layer instead.
=== resized mid-turn ===
[startup=0s session=000000000000000000000000 skills=0p/0g tools=~600t]


 have a look at that


Looking for where the prompt is drawn.

I'll have a look at how the editor draws its prompt.

grep prompt *.go –
=== under a footer ===
[startup=0s session=000000000000000000000000 skills=0p/0g tools=~600t]


 have a look at that


Looking for where the prompt is drawn.

I'll have a look at how the editor draws its prompt.

grep prompt *.go ✓ 1L ~100t
The prompt is drawn by the renderer, so that is the file to read.

Found it. Reading the renderer now.

read cmd/oh/line/render.go ✓ 5L ~100t

The prompt is drawn by layout, which wraps the buffer against the terminal width and reports where
the cursor landed. Nothing there needs changing to make it sticky: the work belongs in the output
layer instead.

> ask something else
> and a second row
